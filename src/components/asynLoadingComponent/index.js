import React from "react";
import { Spin } from "antd";

const AsynLoadingComponent = () => (
  <div style={{ textAlign: "center", marginTop: "100px" }}>
    <Spin />
  </div>
);

export default AsynLoadingComponent;
